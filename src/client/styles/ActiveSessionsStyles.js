import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    sessionHistoryText: {
        textAlign: "center",
        fontSize: 20,
        fontWeight: 500,
        marginTop: 5,
        marginBottom: 10,
    },
    noActiveGroupsText: {
        marginTop: 100,
        textAlign: "center",
        fontSize: 30,
        fontWeight: "bold",
        color: "gray",
    },
});

export default styles;
